# Skip the ts-rs binding export smoke test (it writes src/bindings/).
# Regenerate bindings with:
#   cargo test -p ergo export_typescript_bindings
[profile.default]
default-filter = 'not test(export_bindings)'
